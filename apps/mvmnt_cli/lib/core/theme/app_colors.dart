import 'dart:ui';

class AppColors {
  // Primary colors
  static const Color primaryLight = Color(0xFF00D097);
  static const Color primaryDark = Color(0xFF00E5A8);

  // Secondary colors
  static const Color secondaryLight = Color(0xFF404040);
  static const Color secondaryDark = Color(0xFF9EEEDD);

  // Surface colors
  static const Color surfaceLight = Color(0xFFF8F8F8);
  static const Color surfaceDark = Color(0xFF121212);

  // On Surface colors (text and icons)
  static const Color onSurfaceLight = Color(0xFF121212);
  static const Color onSurfaceDark = Color(0xFFF0F0F0);

  // Background (differentiated for dark mode)
  static const Color backgroundLight = Color(0xFFF5F5F5);
  static const Color backgroundDark = Color(0xFF121212);

  // Text colors
  static const Color textColorLight = Color(0xFF1E1E1E);
  static const Color textColorDark = Color(0xFFE0E0E0);

  // Error color (slightly different for each theme)
  static const Color errorColorLight = Color(0xFFE60026);
  static const Color errorColorDark = Color(0xFFE60026);

  // Optional accent colors
  static const Color accentLight = Color(0xFF00D097);
  static const Color accentDark = Color(0xFF00E5A8);

  // Shimmer colors
  static const Color shimmerLight = Color(0xFFF5F5F5);
  static const Color shimmerDark = Color(0xFF404040);

  // Alert colors
  static const Color alertLight = Color(0xFFFF8F00);
  static const Color alertDart = Color(0xFFFFCA28);
}
